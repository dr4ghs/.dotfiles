# The next line updates PATH for the Google Cloud SDK.
[ -f "$HOME/.local/programs/google-cloud-sdk/path.bash.inc" ] && source "$HOME/.local/programs/google-cloud-sdk/path.bash.inc"

# The next line enables shell command completion for gcloud.
[ -f "$HOME/.local/programs/google-cloud-sdk/completion.bash.inc" ] && source "$HOME/.local/programs/google-cloud-sdk/completion.bash.inc"

